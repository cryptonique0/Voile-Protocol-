[package]
name = "voile-protocol"
version = "0.1.0"
edition = "2021"
description = "A private exit-liquidity layer built on Miden's edge-execution architecture"
license = "MIT"

[dependencies]
sha3 = "0.10"
rand = "0.8"
hex = "0.4"
thiserror = "1.0"

[dev-dependencies]
